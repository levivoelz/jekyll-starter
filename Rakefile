
require 'jekyll/vite'
ViteRuby.install_tasks

task :start do
  exec "./bin/dev"
end

task :build do
  exec "./bin/build"
end

task :preview do
  exec "./bin/preview"
end

task :clean do
  puts "Cleaning up..."
  exec "./bin/clean"
end